export default function UsersPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-4" style={{ color: "var(--gold)" }}>Users (Admin)</h1>
      <p className="text-sm text-gray-400">Admin-only. Manage employee accounts (CRUD, roles, status, password resets). Coming next.</p>
    </div>
  );
}

